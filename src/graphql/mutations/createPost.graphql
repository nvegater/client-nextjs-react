mutation CreatePost ($options: CreatePostInputs!) {
    createPost(options:$options) {
        errors {
            ...ErrorFragment
        }
        post {
            ...PostFragment
        }

    }
}