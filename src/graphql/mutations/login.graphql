mutation Login($options: CredentialsInputs!) {
    login(options: $options) {
        errors {
            field
            message
        }
        user {
            id
            createdAt
            updatedAt
            username
        }
    }
}
